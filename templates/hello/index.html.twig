{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="jumbotron">
            <h1 class="display-4">Bienvenue sur TodoList !</h1>

            {% if app.user %}
                <p class="lead">Bonjour <strong>{{ app.user.email }}</strong> 👋</p>
                <p>Vous êtes bien connecté.</p>
                <hr class="my-4">
                <a class="btn btn-primary btn-lg" href="#" role="button">Voir mes tâches</a>
                <a class="btn btn-danger btn-lg" href="{{ path('app_logout') }}" role="button">Se déconnecter</a>
            {% else %}
                <p class="lead">Gérez vos tâches simplement.</p>
                <hr class="my-4">
                <a class="btn btn-primary btn-lg" href="{{ path('app_login') }}" role="button">Se connecter</a>
                <a class="btn btn-success btn-lg" href="{{ path('app_register') }}" role="button">S'inscrire</a>
            {% endif %}
        </div>
    </div>
{% endblock %}