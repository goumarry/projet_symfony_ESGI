<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}NetBook{% endblock %}</title>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <link rel="icon" href="https://assets.nflxext.com/us/ffe/siteui/common/icons/nficon2016.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #141414;
            color: #b3b3b3;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }

        h1, h2, h3, h4, .text-white-emphasis {
            color: #ffffff;
        }

        .navbar {
            background: linear-gradient(to bottom, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0) 100%);
            padding: 20px 40px;
        }
        .navbar-brand {
            color: #E50914 !important;
            font-size: 1.8rem;
            font-weight: bold;
            letter-spacing: 1px;
        }
        .nav-link {
            color: #b3b3b3 !important;
            transition: color 0.3s;
        }
        .nav-link:hover {
            color: #ffffff !important;
        }

        /* Bouton Principal (Rouge Netflix) */
        .btn-netflix {
            background-color: #E50914;
            color: white;
            border: none;
            border-radius: 4px;
            font-weight: 500;
            transition: background-color 0.2s;
        }
        .btn-netflix:hover {
            background-color: #c11119; /* Rouge plus sombre au survol */
            color: white;
        }

        .card-netflix {
            background-color: rgba(0, 0, 0, 0.75); /* Fond semi-transparent */
            border-radius: 4px;
            border: none;
            /* Ajout d'une ombre subtile pour détacher du fond */
            box-shadow: 0 0 10px rgba(0,0,0,0.5);
        }

        .form-control {
            background-color: #333333;
            border: 0;
            border-radius: 4px;
            color: #ffffff;
            height: 50px;
        }
        .form-control::placeholder {
            color: #8c8c8c;
        }
        .form-control:focus {
            background-color: #454545;
            color: #ffffff;
            box-shadow: none;
            /* Pas de bordure bleue par défaut, mais un soulignement orange subtil */
            border-bottom: 2px solid #E50914;
        }

        a { text-decoration: none; color: #b3b3b3; }
        a:hover { text-decoration: underline; color: #ffffff; }

        input:-webkit-autofill,
        input:-webkit-autofill:hover,
        input:-webkit-autofill:focus,
        input:-webkit-autofill:active{
            -webkit-box-shadow: 0 0 0 30px #333333 inset !important;
            -webkit-text-fill-color: white !important;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_hello') }}">NetBook</a>

        <div class="d-flex align-items-center">
            {% if app.user %}
                <span class="me-3 small d-none d-md-block">
                            {{ app.user.email }}
                        </span>
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_admin') }}" class="btn btn-sm btn-outline-light me-3 border-0">Admin</a>
                {% endif %}
                <a href="{{ path('app_logout') }}" class="btn btn-netflix btn-sm px-3">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-netflix btn-sm px-3">S'identifier</a>
            {% endif %}
        </div>
    </div>
</nav>

<div class="container" style="margin-top: 100px;">
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label == 'error' or label == 'verify_email_error' ? 'danger' : 'success' }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
    {% endfor %}
</div>

{% block body %}{% endblock %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>