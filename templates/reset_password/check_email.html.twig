{% extends 'base.html.twig' %}

{% block title %}Email envoyé{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">
        <div class="card-netflix p-5 text-center" style="width: 500px;">
            <h2 class="mb-4 fw-bold text-white">Email envoyé !</h2>

            <p class="lead mb-4">
                Un email a été envoyé à votre adresse contenant un lien pour réinitialiser votre mot de passe.
            </p>

            <p class="small text-muted mb-4">
                Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                <br>Si vous ne recevez pas d'email, vérifiez vos spams.
            </p>

            <a href="{{ path('app_login') }}" class="btn btn-netflix px-4">Retour à l'accueil</a>
        </div>
    </div>
{% endblock %}